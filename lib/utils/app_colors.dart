// App color constants
import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF175DA9); // Bajaj Blue
  static const Color primaryDark = Color(0xFF0D3A6B);
  static const Color primaryLight = Color(0xFF2D7AC7);

  // Accent colors
  static const Color accent = Color(0xFFFF5722); // Orange
  static const Color accentLight = Color(0xFFFF7043);

  // Background colors
  static const Color background = Color(0xFFF5F5F5);
  static const Color cardBackground = Colors.white;

  // Text colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textWhite = Colors.white;

  // Other colors
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFF9800);
  static const Color error = Color(0xFFF44336);
  static const Color info = Color(0xFF2196F3);

  // Banner colors
  static const Color bannerPeach = Color(0xFFFFD4B2);
  static const Color bannerOrange = Color(0xFFFF6B35);
}
